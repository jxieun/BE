spring.application.name=jeonbongjun
server.port=8080

# Database 설정 (Render PostgreSQL)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/roboadvisor}
spring.datasource.username=${DB_USERNAME:admin}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA 설정
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Redis 설정 (Render Redis)
spring.data.redis.url=${REDIS_URL:redis://localhost:6379}
spring.session.store-type=redis

# Redis Health Check
management.health.redis.enabled=true
management.endpoint.health.show-details=never

# API Keys 
deepsearch.api.base-url=https://api-v2.deepsearch.com
deepsearch.api.key=${DEEPSEARCH_API_KEY:d971def21a2d4c7682f5e340ec17eafc}
alphavantage.api.key=${ALPHAVANTAGE_API_KEY:89VFYOD5PPU07O22}
ai.api.base-url=${AI_API_BASE_URL:http://localhost:8000}